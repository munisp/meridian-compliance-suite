name: ci

on:
  push:
    branches: [main, "hardening/*"]
  pull_request:

jobs:
  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.23.4"
      - name: root module (packages + einvoicing)
        run: |
          go build ./... && go vet ./... && go test -race ./...
      - name: case-mgmt
        working-directory: services/case-mgmt
        run: go build ./... && go vet ./... && go test -race ./...
      - name: pos-vat
        working-directory: services/pos-vat
        run: go build ./... && go vet ./... && go test -race ./...
      - name: vasp-carf
        working-directory: services/vasp-carf
        run: go build ./... && go vet ./... && go test -race ./...

  pack-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: checkout canonical meridian-rule-packs
        uses: actions/checkout@v4
        with:
          repository: munisp/meridian-rule-packs
          path: meridian-rule-packs
      - name: embedded packs must mirror canonical (drift guard)
        run: python3 tools/check_embedded_sync.py --canonical meridian-rule-packs

  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: etr pytest
        working-directory: services/etr
        run: |
          pip install --user -r requirements.txt
          python3 -m pytest -q
      - name: wht pytest
        working-directory: services/wht
        run: |
          pip install --user -r requirements.txt
          python3 -m pytest -q
      - name: insights pytest
        working-directory: services/insights
        run: |
          pip install --user -r requirements.txt
          python3 -m pytest -q

  portal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: compliance-portal build
        working-directory: portals/compliance-portal
        run: |
          npm ci
          npm run build
